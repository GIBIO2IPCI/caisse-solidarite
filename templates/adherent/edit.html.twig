{% extends 'base.html.twig' %}

{% block title %}Edit Adherent{% endblock %}

{% block body %}
   <div class="container">
       <div class="d-flex justify-content-between align-items-center">

           <div class="">
               <h1 class="mb-4">Edit Adherent</h1>
           </div>

           <div class="d-flex">
               <a class="btn btn-info text-dark me-3" href="{{ path('app_adherent_index') }}">
                   <i class="fas fa-arrow-left"></i>
                   back to list
               </a>
               {{ include('adherent/_delete_form.html.twig') }}
           </div>

       </div>

       {{ include('adherent/_form.html.twig', {'button_label': 'Update'}) }}

   </div>
{% endblock %}
