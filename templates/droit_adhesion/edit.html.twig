{% extends 'base.html.twig' %}

{% block title %}Edit DroitAdhesion{% endblock %}

{% block body %}

    <div class="container">
        <div class="d-flex justify-content-between align-items-center">

            <div class="mb-4">
                <h1>Modifier une adhésion</h1>
            </div>

            <div class="d-flex">
                <a class="btn btn-info me-3" href="{{ path('app_droit_adhesion_index') }}">
                    <i class="fa-solid fa-arrow-left"></i>
                    retour à la liste
                </a>
                {{ include('droit_adhesion/_delete_form.html.twig') }}
            </div>

        </div>

        {{ include('droit_adhesion/_form.html.twig', {'button_label': 'Modifier'}) }}

    </div>
{% endblock %}
