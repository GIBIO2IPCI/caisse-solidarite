{{ form_start(form) }}
    <div class="row">
        <div class="col-6 mb-4">
            {{ form_row(form.adherent) }}
        </div>
        <div class="col-6 mb-4">
            {{ form_row(form.date_cotisation) }}
        </div>
    </div>
    <button class="btn btn-primary">
        <i class="fa-solid fa-save"></i>
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}