{% extends 'base.html.twig' %}

{% block title %}Nouvelle cotisation{% endblock %}

{% block body %}

    <div class="container">

        <div class="d-flex justify-content-between align-items-center">

            <div class="">
                <h1 class="mb-4">Nouvelle cotisation</h1>
            </div>

            <div class="d-flex">
                <a class="btn btn-info me-3" href="{{ path('app_cotisation_index') }}">
                    <i class="fas fa-arrow-left"></i>
                    Retour à la liste
                </a>
            </div>

        </div>

        {{ include('cotisation/_form.html.twig') }}

    </div>

{% endblock %}
