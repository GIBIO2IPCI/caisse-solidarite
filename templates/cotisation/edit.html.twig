{% extends 'base.html.twig' %}

{% block title %}Modifier une cotisation{% endblock %}

{% block body %}

    <div class="container">
        <div class="d-flex justify-content-between align-items-center">

            <div class="">
                <h1 class="mb-4">Modifier une cotisation</h1>
            </div>

            <div class="d-flex">
                <a class="btn btn-info mx-4" href="{{ path('app_cotisation_index') }}">
                    <i class="fa-solid fa-arrow-left"></i>
                    retour à la liste
                </a>
                {{ include('cotisation/_delete_form.html.twig') }}
            </div>

        </div>

        {{ include('cotisation/_form.html.twig', {'button_label': 'Modifier'}) }}

    </div>

{% endblock %}